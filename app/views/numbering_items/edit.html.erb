<h2><%=h l(:label_numbering_document_edit) %></h2>
<%= labelled_form_for :numbering_item, @numbering_item, :url => project_numbering_item_path(@project, @numbering_item.id) do |f| %>
	<%= render :partial => 'numbering_items/form', :locals => {:form => f} %>
	<%= f.submit l(:button_create) %>
<% end %>
